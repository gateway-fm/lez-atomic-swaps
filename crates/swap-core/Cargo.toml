[package]
name = "lez-swap-core"
version = "0.1.0"
description = "Chain-independent protocol state machine for LEZ atomic swaps"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1.0.219", features = ["derive"] }
sha2 = "=0.10.9"
thiserror = "2.0.12"

[dev-dependencies]
proptest = { version = "1.11.0", default-features = false, features = ["std"] }
serde_json = "1.0.150"
