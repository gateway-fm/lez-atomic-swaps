From 9edd9727a9ea01b3ccf4d9b927bbf7345d1f9b72 Mon Sep 17 00:00:00 2001
From: Igor Mandrigin <i@mandrigin.ru>
Date: Tue, 18 Aug 2026 14:16:33 +0200
Subject: [PATCH 23/26] local: align UI ZEC fixture validity with offer TTL

---
 crates/zec-swap-sdk/src/bin/zec-ui-fixture.rs | 5 ++++-
 1 file changed, 4 insertions(+), 1 deletion(-)

diff --git a/crates/zec-swap-sdk/src/bin/zec-ui-fixture.rs b/crates/zec-swap-sdk/src/bin/zec-ui-fixture.rs
index fa019f3..96ab69f 100644
--- a/crates/zec-swap-sdk/src/bin/zec-ui-fixture.rs
+++ b/crates/zec-swap-sdk/src/bin/zec-ui-fixture.rs
@@ -42,6 +42,9 @@ struct Arguments {
     /// Exact swap id bound into the agreement.
     #[arg(long)]
     swap_id: String,
+    /// Validity window for the source agreement used during Chat acceptance.
+    #[arg(long, default_value_t = 7_200)]
+    valid_for_seconds: u64,
 }
 
 fn write_private(path: &Path, bytes: &[u8]) -> Result<()> {
@@ -158,7 +161,7 @@ fn main() -> Result<()> {
             40,
         ),
         ZecRefundPlanV1::new(basis_time, 116, (basis_time + 60) * 1_000, basis_time + 90),
-        NegotiationTranscriptV1::new([9; 32], [10; 32], basis_time + 300),
+        NegotiationTranscriptV1::new([9; 32], [10; 32], basis_time + arguments.valid_for_seconds),
     );
     let commitment = body.commitment();
     let record = ZecAgreementRecordV1::from_parts(
