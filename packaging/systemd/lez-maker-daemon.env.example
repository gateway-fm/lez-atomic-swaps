# Copy to /etc/lez-atomic-swaps/zec-actor.env with mode 0600 after installing.
# This is the SHA-256 of the exact installed /usr/bin/zec-reference-actor file.
# Do not add shell quotes or whitespace.
ZEC_ACTOR_PROGRAM_SHA256=REPLACE_WITH_64_HEXADECIMAL_CHARACTERS
